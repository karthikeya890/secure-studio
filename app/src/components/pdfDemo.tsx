import { Flex } from "@chakra-ui/react"
const Invoice = () => {
    return (
        <Flex>
            {/* Invoice Header */}
            {/* Invoice Body */}
            {/* Invoice Footer */}
        </Flex>
    )
}

export default Invoice